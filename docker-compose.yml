services:
  app:
    image: wap/glifestream:latest
    build: .
    volumes:
        - ./run:/app/run
        - ./run/media:/app/media
    ports:
        - "8080:80"
  memcached:
      image: memcached:1.6
      entrypoint:
          - memcached
          - -m 2
