[pytest]
DJANGO_SETTINGS_MODULE = glifestream.settings
norecursedirs = run .visual-regression
markers =
    e2e: end-to-end browser tests that use Playwright and the live server
filterwarnings =
    ignore:Error when trying to teardown test databases:pytest.PytestWarning
